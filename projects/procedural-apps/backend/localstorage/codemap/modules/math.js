require("jquery")
require("mithril")
require("mathquill")

module.exports = {
    sequence: {
        type: 'prototype',
        name: 'Expression',
        description: 'Write a formula with LaTeX, variables automatically generate sockets'
    }
}